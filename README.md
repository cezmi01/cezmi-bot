# cezmi-bot

Bu repo icinde web panel `panel/` klasorundadir.

Calistirmak icin:
1) Node.js 18+ kurulu olmali.
2) `cd panel`
3) `npm install`
4) `npm run dev`

Terminalde "package.json bulunamadi" gibi bir hata gorursen,
komutlari `panel/` klasoru icinde calistirdigini kontrol et.