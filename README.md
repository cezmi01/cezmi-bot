# cezmi-bot

## Setup

- Install Python dependencies: `python -m pip install -r requirements.txt`
- The `telebot` import is provided by `pyTelegramBotAPI`.

## Coin Config Dashboard

- Update `helpers.py` with your `symbols_for_exchange` data.
- Run: `python coin_config_dashboard.py`
- Open `http://localhost:5000`.